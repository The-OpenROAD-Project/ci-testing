# Queue refs observed by scripts/watch-queue.sh. GitHub deletes them seconds
# after an entry merges, so they have to be recorded locally for
# scripts/verify-merge.sh to compare against afterwards.
.mq-queue-log
.mq-watch.pid
ci/config.env.tmp
